# nestjs+prisma7+jwt

实现jwt鉴权 然后有个邮箱服务，加上验证码，redis缓存怎么实现？redis用来动态缓存验证码和token
token不用存在服务端
增加文章删除文章更改查询

想把东西暴露出去的时候，记得先提供：providers: [JwtStrategy,AuthService],然后再暴露

# jwt

思路是由一个auth的模块来管
里面暴露一个service，里面有创建token的方法
然后暴露一个strategy，这个感觉就是调用validate的，但是很奇怪，我都没把这个strategy暴露出去，其他的模块里面竟然还是能用？
然后在user的模块里面使用imports导入auth模块，这里要注意，模块里面的service要用到其他模块的service的时候，要先把module导入一下
然后再创建guards守卫，这个里面创建一个守卫暴露出来，里面啥也不用谢，直接继承一下就行了：extends AuthGuard('jwt')
很神奇，然后全局注入一下守卫，就能鉴权了，何意味

app的module里面还要注册一下这个：这个是全局的
```ts
JwtModule.registerAsync({
              useFactory: async ()=>({
                secret:process.env.TOKEN_SECRET_KEY,
                signOptions:{expiresIn:'15m'}
              }),
              global:true
            })
```


```ts
@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {
    private wihteList = ['/user/login']
    //这个函数是用来判断当前请求是否需要进行身份验证的，如果请求的URL在白名单中，就直接放行，否则就调用父类的canActivate方法来验证用户的token是否合法
    canActivate(context: ExecutionContext): boolean | Promise<boolean> | Observable<boolean> {
        const req = context.switchToHttp().getRequest();
        if(this.wihteList.includes(req.url)){
            return true;
        }
        return super.canActivate(context);//这是给AuthGuard放行，也就是调用AuthGuard的canActivate方法，来验证用户的token是否合法，如果合法就放行，否则就拒绝访问
    }
}
```
# 注册服务
可以用nodemailer来注册，然后把验证码放在redis里面进行验证？
发送验证码应该是一个独立的模块，但是点击之后的验证环节应该放在哪里？放在user里面？
点击获取验证码=>传入注册时使用的email=>创建code并发送邮件=>code存到redis里面=>客户拿到，输入并点击注册时从redis里面拿到开始验证
模块：
1. nodemailer
2. redis
3. user



JWT 验证通过后，用户信息会自动放到 request.user 里



jwt鉴权，发布文章，获取文章暂时完成了




实现：
 我在实现的时候遇到了很多困难，我需要记录下来
 写这个实际的项目的时候总是非常的奇怪，感觉是的不到位

1. redis和鉴权我不知道放哪去，怎么实例
2. redis是使用useFacturay动态注册的，这个和prisma的那个有什么区别？
3. 全局的app.useGlobalGuards(new JwtAuthGuard())//全局JWT验证当时为什么没有起作用？
4. app.useGlobalGuards(new JwtAuthGuard())  怎么设置对应的路由不需要验证
5. imports provides controller exports到底是什么关系
6. 一个请求的生命周期是什么
7. providers: [{
    provide:'REDIS_CLIENT',
    useFactory:()=>{
      return new Redis({
        host:process.env.REDIS_HOST,
        port:Number(process.env.REDIS_PORT)
      })
    }
  },RedisService],这里都动态注册了，按理来说只要要用的时候注入就行了，为啥还要RedisService？



# 鉴权的重复登录
如果我重复登录，然后有多个token，这样子的话都会生效，这样，我把生成的token放到黑名单里面，每次验证token的时候，就去黑名单里面找，这样就好了。但是这又有一个问题，比如有两个接口都需要token，那登录之后去验证不就不能访问接口了吗


存在set里面，重启服务会刷新set这个集合里面的数据



## 流程
liveserver打开register，发送请求，获取验证码，这里设置了mail的服务去发送验证码，同时把验证码存到redis里面300秒，